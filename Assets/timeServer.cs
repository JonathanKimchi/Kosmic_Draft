using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class timeStorage : MonoBehaviour
{
    // Start is called before the first frame update
    //GameObject[] storeTime = new GameObject[1000];
    //int timeTracker = 0;

    void Start()
    {
        
    }
    // Update is called once per frame
    void Update()
    {
        
    }
    public void addTimeBone(string input1)
    {
        //storeTime[timeTracker] = GameObject.Find(input1);
        //timeTracker++;//change to hashmap down the line, possibly.  Almost definitely not needed.
    }
}
